def main() -> None:
    """
    미래 Streamlit prototype의 진입점 skeleton.

    예정 흐름:
    1. state 초기화
    2. api_client로 backend API 호출
    3. UIEvent 목록 수신
    4. components에 렌더링 위임

    금지:
    - 현재 단계에서 streamlit import 금지
    - 기존 app.py 복사 금지
    - route_engine 직접 import 금지
    - Kakao/Weather API 직접 호출 금지
    """
    pass
