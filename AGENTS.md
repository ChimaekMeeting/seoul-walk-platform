# ROUDI 저장소 작업 규칙

이 파일은 사람과 AI가 같은 기준으로 백엔드 작업을 나누고 연결·검증하기 위한 저장소 공통 지침이다.

## 작업 시작

백엔드 코드나 문서를 조사·변경하기 전에 다음 순서를 따른다.

1. `git status --short`로 기존 변경을 확인하고 사용자 변경을 보존한다.
2. 작업 단위 계약의 `필수 읽기`에 지정된 문서만 읽는다. 보통 담당 영역 계약 1개이며, 필요할 때만 Workflow 또는 operations 문서가 추가된다.
3. 경계가 불명확한 조사·작업 배분·통합 작업에서만 `system_overview.md`와 `backend_domain_coverage.md`를 읽는다.
4. 문서를 새로 만들거나 이동할 때만 `docs/README.md`와 `documentation_guide.md`를 읽는다.
5. 할 일을 다음 두 종류로 구분한다.
   - 새 문서가 필요한 누락 계약
   - 이미 만든 Current 문서를 코드·실행 결과로 확정하거나 갱신하는 일
6. 폴더 이름만으로 책임을 추정하지 않고 현재 진입점과 호출 관계를 코드에서 확인한다.

기본값으로 `docs/` 전체를 읽지 않는다. Workflow와 operations 문서는 작업 단위 계약에 정확한 파일이 지정된 경우에만 읽는다.

## 문서 역할

- `docs/`: 현재 팀이 사용하는 구조·계약·실행 기준
- `docs/proposals/`: 아직 구현하지 않았거나 승인되지 않은 변경안
- `analysis/`: 재현 가능한 실험 과정과 사용자가 보존하기로 승인한 판단 근거

AI 조사 결과는 먼저 채팅이나 리뷰로 보고한다. 사용자가 판단하기 전에 AI의 수용·거절 결론을 `analysis/`에 확정 기록하지 않는다.

Current 문서에는 현재 코드로 확인한 사실만 쓴다. 실행 수치는 확인 날짜·환경·재현 위치를 함께 적고, 일회성 관측값을 고정 기대값처럼 표현하지 않는다.

## 영역과 Workflow

- 영역 계약은 `docs/{domain}/`에서 책임·입력·출력·진입점·의존성·영향·복구·검증·완료 기준을 관리한다.
- Workflow는 `docs/architecture/workflows/`에서 여러 영역이 실제 요청에서 연결되는 순서와 실행 증거를 관리한다.
- `src`의 기술 계층 폴더를 `docs`에 그대로 복제하지 않는다. 실제 독립 책임과 변경 경계를 기준으로 영역을 정한다.
- 같은 계약을 여러 문서에 반복하지 않고 단일 기준 문서에 링크한다.

## 병렬 작업과 문서 충돌

- 한 작업 단위는 한 담당 영역과 허용된 코드·문서 경로를 가진다.
- 도메인 작업자는 기본적으로 자신의 `docs/{domain}/`만 수정한다.
- `docs/README.md`, `documentation_guide.md`, `architecture/system_overview.md`, `architecture/system_workflows.md`, `architecture/backend_domain_coverage.md`, `templates/`는 공유 문서다.
- 공유 문서는 작업에 소유권이 명시된 담당자 또는 통합 담당자만 수정한다.
- 개별 Workflow는 같은 기간에 한 명만 수정한다. 다른 영역 작업자는 필요한 변경을 작업 종료 보고의 `공유 문서 인계`에 남긴다.
- 독립 영역 브랜치를 먼저 병합하고, Workflow와 공유 지도는 통합 담당자가 마지막에 갱신한다.
- 모든 문서를 한 브랜치에서 관리할 필요는 없다. 같은 공유 파일을 여러 브랜치가 동시에 수정하지 않도록 소유권을 나눈다.

## 변경과 검증

1. 변경 전 입력·출력·의존 영역과 저장소 영향을 적는다.
2. 가장 작은 독립 작업 단위로 변경한다.
3. 관련 단위 테스트와 Workflow 검증을 실행한다.
4. API 응답만 보지 않고 필요한 경우 PostgreSQL·Valkey·Graph·외부 호출 결과를 확인한다.
5. 코드 계약이 달라졌다면 같은 작업에서 Current 문서를 갱신한다.
6. 새 문서·이동·삭제 후 `docs/README.md`의 지도와 상대 링크를 확인한다.
7. 완료 표시는 코드 대조와 요구된 검증 결과가 있을 때만 사용한다.

## 안전 경계

- 작업 범위 밖의 사용자 변경을 수정·삭제·되돌리지 않는다.
- backend 하네스 문서 작업에서는 명시적으로 요청되지 않은 `frontend/**`를 조사·수정하지 않는다.
- 파괴적 실행과 데이터 전체 재구축 전에는 대상·트랜잭션·백업·복구 절차를 확인한다.
- 스테이징·커밋·푸시는 사용자가 명시적으로 요청할 때만 수행한다.
- 비밀값·토큰·개인정보를 문서·로그·테스트 산출물에 남기지 않는다.

## 작업 종료 보고

- 변경하거나 확인한 계약
- 실행한 검증과 확인하지 못한 항목
- 실패 시 복구 시작점
- 남은 사용자 판단
- 스테이징·커밋 여부

위 항목을 구분해 보고한다.
